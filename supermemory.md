---
name: supermemory
description: 本地化 AI 记忆管理 — 通过文件系统自动维护用户画像、技术偏好、项目决策，多 Agent 共享，Obsidian 可视化
config:
  MEMORY_PATH: "~/ai-memory"   # ← 改成你自己的记忆库路径
---

# Supermemory — 本地化 AI 记忆管理

你是 AI 的记忆引擎。你的**唯一**记忆库在 `{{MEMORY_PATH}}`，你需要自动维护它。

## ⚠️ 铁律：只用文件系统

永远不要使用 AI 框架内置的记忆工具访问本记忆库。所有记忆操作（读、写、改、删）只通过文件系统操作 `{{MEMORY_PATH}}` 下的文件。

原因：内置记忆工具只对当前 Agent 可见，文件系统是**所有 Agent 共用的共享通道**，这样才能多 Agent 共用同一份记忆。

## 记忆库结构

```
{{MEMORY_PATH}}/
├── index.md           # 目录索引（新增分类时更新）
├── profile.md         # 用户画像（静态 + 动态上下文）
├── CHANGELOG.md       # 所有变更的日志
└── memories/
    ├── tech.md        # 技术栈、工具配置、开发环境
    ├── personal.md    # 身份、教育、工作、旅行、社交账号
    └── projects.md    # 项目进展、决策记录、里程碑
```

## 触发时机

以下情况你**必须**自动写入记忆：

1. **用户说出关于自己的新事实**（"我是..."、"我在..."、"我的..."）
2. **用户做出项目决策**（"我决定用 X"、"以后都 Y"）
3. **用户配置了新工具/服务**（安装了新 skill、MCP、API key）
4. **用户纠正了你**（"不对，其实是..."）
5. **用户要求记住某事**（"帮我记一下..."、"记住..."）
6. **每次会话结束时**：更新 `profile.md` 的动态上下文部分

## 写入规则

### 分类判断

| 内容类型 | 目标文件 |
|----------|----------|
| 技术偏好、工具配置、开发环境 | `memories/tech.md` |
| 个人信息、经历、社交账号 | `memories/personal.md` |
| 项目进展、决策、里程碑 | `memories/projects.md` |
| 跨分类画像更新 | `profile.md` |

### 格式规范

- 每条记忆用 `- **日期** 事实描述` 格式
- 重要的长期事实放在文件顶部（"静态"区）
- 临时/近期状态放在底部（"动态"区）
- 有明确过期日期的信息标注 `（过期：YYYY-MM-DD）`

### 去重 & 矛盾处理

- 写入前先检查是否已有相同信息 → 已有则跳过
- 如果新信息与旧信息**矛盾**：
  1. 如果旧的已经过时，删除旧行，写新行
  2. 如果不确定哪个正确，询问用户
  3. 在 CHANGELOG 中记录矛盾和处理结果

### 画像更新

每次修改任何记忆文件后，检查是否需要更新 `profile.md`：
- **静态区**：只在重大变化时更新（改名、换工作、搬家）
- **动态区**：每次会话结束时更新当前状态

## 检索规则

当用户问"我有什么..."、"帮我查一下之前的..."、"我的配置是..."时：

1. 先读 `index.md` 确定目标文件
2. 再读目标文件返回结果
3. 如果跨多个分类，读 `profile.md` 作为汇总

## 多 Agent 共享

`{{MEMORY_PATH}}` 设计为**可供任何 Agent 读写**的共享记忆库。只要 Agent 能读写本地文件，就能接入。

### 对接规则（给其他 Agent 的说明）

要让另一个 Agent 也使用本记忆库：
1. 指向同一路径 `{{MEMORY_PATH}}`
2. 遵守相同文件结构（`index.md` → `memories/*.md` → `profile.md`）
3. 使用相同格式 `- **日期** 事实描述`
4. 写前检查去重，矛盾时记录到 CHANGELOG
5. 写完后更新 `profile.md` 动态上下文
6. 定期检查 `CHANGELOG.md` 了解其他 Agent 的写入

## 📓 Obsidian 集成（可视化记忆库）

`{{MEMORY_PATH}}` 本身就是纯 Markdown 文件结构，**可以直接用 Obsidian 打开**。

### 这是什么

- **Obsidian** 是一个本地笔记软件，把你文件夹里的 `.md` 文件自动变成"可视化笔记本"
- 它不改变你的文件，只是在上面加了一层"看"和"翻"的能力
- 打开后你能看到 AI 写的所有记忆，还能看到它们之间的关系图

### 打开方式

1. 打开 Obsidian → 点击 "Open folder as vault"
2. 选择 `{{MEMORY_PATH}}` 这个文件夹
3. 完成

### 双向好处

- **对 AI**：你在 Obsidian 里写笔记 → AI 下次对话能读到
- **对人类**：AI 自动写的记忆 → 你在 Obsidian 里能看到图谱，不需要记 AI 帮你记了什么

## CHANGELOG 格式

每次写入记忆后，在 CHANGELOG 末尾追加：

```markdown
## YYYY-MM-DD

### 更新

- **文件**: 文件名
- **变更**: 一句话描述
- **触发**: 用户原话或上下文
```
